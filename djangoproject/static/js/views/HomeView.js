// Generated by CoffeeScript 1.3.3
var TodosCollection, sortedByAlphabet;

window.WiserWater.HomeView = Backbone.View.extend({
  initialize: function(options) {
    this.template = _.template(WiserWater.tpl.get('home'));
    return this;
  },
  events: {
    "click .lakeId": "onLakeClick"
  },
  render: function() {
    $(this.el).html(this.template());
    return this;
  },
  onLakeClick: function(args) {
    args.preventDefault();
    return WiserWater.app.renderLake();
  }
});

TodosCollection = new Backbone.Collection();

TodosCollection.add([
  {
    title: "go to Belgium.",
    completed: false
  }, {
    title: "go to China.",
    completed: false
  }, {
    title: "go to Austria.",
    completed: true
  }
]);

TodosCollection.forEach(function(model) {
  return console.log(model.get("title"));
});

sortedByAlphabet = TodosCollection.sortBy(function(todo) {
  return todo.get("title").toLowerCase();
});

console.log("- Now sorted: ");

sortedByAlphabet.forEach(function(model) {
  return console.log(model.get("title"));
});
